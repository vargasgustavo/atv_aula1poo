public interface Magia {
    // vamos criar a interface para os personagens se curar
    // vamos fazer elas serem publique e abstratas

    void curar();
    void fireball(Personagem alvo);
}
